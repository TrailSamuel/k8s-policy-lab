apiVersion: v1
kind: Pod
metadata:
  name: bad-host-path
spec:
  containers:
    - name: app
      image: nginx:alpine
      volumeMounts:
        - name: host
          mountPath: /host
  volumes:
    - name: host
      hostPath:
        path: /
        type: Directory
